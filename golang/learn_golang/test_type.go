package main

import (
	"fmt"
	// "unsafe"
	"math"
)

func main() {

	//string
	// s := "hello"
	// t := s[2:3]
	// fmt.Println(s, t)
	// //slice
	// x := []int{1, 2, 3, 4, 5}
	// y := x[2:5]

	// x[2] = 10

	// x = append(x, 3)
	// fmt.Println(len(x), cap(x))

	// fmt.Println(x, y)

	// m := make([]byte, 100)
	// ptr := unsafe.Pointer(&s[0])

	fmt.Println(math.Ceil(3 * 1.0 / 5))
}
